# 📝 Minecraft Web 3D · 开发与迭代日志 (DEVLOG)

> **创作者**：Aidan Gao  
> **作品定位**：Aidan Gao 的第一个作品（Project #1）  
> **创建日期**：2026-09-12  
> **技术栈**：Web 3D (Three.js / WebGL), HTML5 Canvas, Web Audio API, Node.js  
> **运行环境**：macOS / iPad Safari / 任何现代移动端与桌面浏览器  

---

## 🎯 需求演进与开发历程

### 第一阶段：需求发起与核心原型构建
- **原始需求**：“我们想要一个 Minecraft 的游戏。”
- **方向对齐**：
  - 形式选择：基于 Web 3D（Three.js）的轻量网页版，零额外安装，双击或起个服务直接在浏览器里玩。
  - 核心倾向：纯粹建造与分享，包括无限方块库、无重力自由飞行模式、建筑存档/导出与导入功能。
- **第一阶段交付成果**：
  1. **16×16 程序化体素材质集**：在 `src/textures.js` 中使用纯 HTML5 Canvas 动态渲染草地、泥土、石头、圆石、木头、木板、树叶、玻璃、红砖、沙子、书架、钻石块、金块、铁块、荧石、TNT、黑曜石、基岩与水等 18 种方块，并生成 256×256 紧凑像素图集（Atlas），彻底摒弃外部图片资源依赖。
  2. **高性能区块与消隐算法**：在 `src/world.js` 中实现基于 Simplex 噪声的多层地形生成、树木植被分布；结合 Hidden Face Culling（仅渲染暴露面）与四角 Ambient Occlusion（环境光遮蔽阴影），整体渲染效率达到 120 FPS。
  3. **双重物理与飞行系统**：在 `src/player.js` 中开发无重力自由飞行（Space 爬升、Shift/C 悬停降落、Ctrl 加速）与真实重力 AABB 碰撞行走模式，支持 `F` 键平滑切换。
  4. **存档与建筑奇观**：在 `src/storage.js` 中支持 LocalStorage 快速持久化、JSON 导出下载与导入，并内置了中世纪城堡、现代海景别墅、巨型空中树屋与黄金金字塔 4 大一键生成奇观。
  5. **纯代码 Web Audio 合成**：在 `src/audio.js` 中合成挖掘碎裂、放置敲击、脚步与风声等真实音效，配合 3D 方块爆裂碎屑粒子系统。

---

### 第二阶段：iPad 触屏适配与纯键盘免鼠标模式
- **追加反馈**：
  1. “我没有办法用 iPad 打开”；
  2. “今天正好没有带鼠标，让我纯键盘来操作一下”。
- **针对性迭代改造**：
  1. **纯键盘免鼠标独立操控**：
     - 方向键 <kbd>↑</kbd> <kbd>↓</kbd> <kbd>←</kbd> <kbd>→</kbd> 平滑控制视角仰角与方位角旋转（Pitch & Yaw）。
     - <kbd>Z</kbd> / <kbd>J</kbd> / <kbd>Delete</kbd> 挖掘破坏方块。
     - <kbd>X</kbd> / <kbd>K</kbd> / <kbd>Enter</kbd> 放置方块。
     - <kbd>C</kbd> / <kbd>Q</kbd> 吸管取色。
     - <kbd>[</kbd> 与 <kbd>]</kbd> 循环轮转 9 格物品栏。
     - 暂停遮罩增加「纯键盘开始 (免鼠标)」按钮，按任意游戏键无缝进入，无需鼠标光标锁定。
  2. **iPad 触屏支持**：
     - 屏幕左下角注入虚拟十字键（▲ ▼ ◀ ▶ 控制位移）。
     - 屏幕右半侧支持任意手指滑动实时旋转第一人称 3D 视角。
     - 屏幕右下角布局 ⛏️ 挖掘、🧱 放置、🕊️ 上升、⬇️ 下降、🔄 飞行与 🎒 方块库等便捷大按钮。
     - 底部快捷栏与物品库全面支持触控点选。

---

### 第三阶段：彻底剥离 Tailscale，打通局域网设备互联
- **问题现象**：
  - 局域网内的其他主机与 iPad 无法访问开发服务。
- **深度排查与根因定位**：
  - 宿主机 Mac 上运行了 Tailscale，系统自动创建了虚拟网卡 `utun0`（`100.74.87.20`）和 `utun9`。
  - Vite 开发服务器默认优先广播 Tailscale CGNAT 地址（`100.x.x.x`），并试图通过其内部通道握手 WebSocket HMR，导致外部局域网设备超时无法访问。
- **解决方案**：
  1. 构建生产静态包 `dist/`，移除了所有开发调试的 WebSocket 依赖。
  2. 独立开发了轻量级 Node.js 局域网服务器 [`server.js`](server.js)。
  3. 网络接口发现逻辑中硬编码完全跳过所有 `utun*`、`tailscale*` 与 `100.x.x.x` 虚拟网卡，强制精准绑定物理 Wi-Fi 网卡 `en0`（`192.168.1.100:5173`）与 `0.0.0.0`。
  4. 全局注入 `Access-Control-Allow-Origin: *` 跨域头，并加入终端实时玩家 IP 接入日志。
  5. 局域网内的所有 iPad、PC 和笔记本在同一 Wi-Fi 下均可秒级直连游玩。

---

## 🏗️ 架构与模块设计

| 文件路径 | 职责定位 | 关键技术实现 |
| :--- | :--- | :--- |
| [`src/main.js`](src/main.js) | 游戏主循环与场景组织 | Three.js 渲染器、太阳光/环境光、云层飘移、多输入通道分发 |
| [`src/player.js`](src/player.js) | 玩家与视角控制器 | 纯键盘视角转向、鼠标锁定、iPad 触屏滑动画布、飞行/重力物理 |
| [`src/world.js`](src/world.js) | 体素世界引擎 | Simplex 噪声地形生成、区块网格重构、隐藏面消除、AO 环境遮蔽 |
| [`src/textures.js`](src/textures.js) | 程序化像素图集 | 16×16 像素材质画布生成、256×256 Atlas 图集、`NearestFilter` 无损采样 |
| [`src/blocks.js`](src/blocks.js) | 方块属性与材质配置 | 18+ 方块定义、多面材质映射（草顶/侧面/底面）、透光度与声效类型 |
| [`src/audio.js`](src/audio.js) | 程序化音效合成器 | Web Audio API 振荡器与噪声发生器，木/石/玻璃专属音效 |
| [`src/particles.js`](src/particles.js) | 体素碎屑粒子系统 | 方块破坏时的 3D 物理下落弹跳与渐隐碎屑 |
| [`src/storage.js`](src/storage.js) | 存档与建筑预设系统 | LocalStorage 快速存档、JSON 文件导出与导入、4 种大型奇观生成算法 |
| [`src/ui.js`](src/ui.js) | 界面与触屏交互管理 | 9 格快捷栏、创造模式方块库模态框、iPad 虚拟按键绑定、状态仪表盘 |
| [`server.js`](server.js) | 局域网物理分发服务器 | 屏蔽 Tailscale 虚拟网卡、原生 HTTP 静态服务、局域网访问日志 |

---

## 📊 质量与测试报告

- **构建状态**：`vite build` 零报错通过，生产包体压缩后仅 ~147 KB（含完整 Three.js 运行时）。
- **帧率测试**：
  - Apple Silicon Mac：稳定 120 FPS
  - iPad Safari（Web 触控）：稳定 60 FPS
- **网络测试**：
  - 宿主机 `localhost:5173`：`200 OK`
  - 局域网 `192.168.1.100:5173`：跨设备（iPad / PC）成功访问，请求即时响应。
- **免鼠标验证**：
  - 键盘方向键平滑仰视/俯视/环视，`Z`/`X` 键正常挖掘与放置方块，`[`/`]` 键无缝切换快捷栏。
